{
  "name": "qip-project-site",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "test": "node --test site.test.mjs",
    "verify": "npm run build && npm test"
  }
}
